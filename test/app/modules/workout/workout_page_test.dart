import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('WorkoutPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(WorkoutPage(title: 'Workout')));
    //  final titleFinder = find.text('Workout');
    //  expect(titleFinder, findsOneWidget);
  });
}
