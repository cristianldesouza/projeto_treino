import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:projeto_treino/app/modules/workout/workout_module.dart';

void main() {
  initModule(WorkoutModule());
  // WorkoutController workout;
  //
  setUp(() {
    //     workout = WorkoutModule.to.get<WorkoutController>();
  });

  group('WorkoutController Test', () {
    //   test("First Test", () {
    //     expect(workout, isInstanceOf<WorkoutController>());
    //   });

    //   test("Set Value", () {
    //     expect(workout.value, equals(0));
    //     workout.increment();
    //     expect(workout.value, equals(1));
    //   });
  });
}
