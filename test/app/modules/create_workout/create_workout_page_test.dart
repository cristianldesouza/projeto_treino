import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:projeto_treino/app/modules/create_workout/create_workout_page.dart';

void main() {
  testWidgets('CreateWorkoutPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CreateWorkoutPage(title: 'CreateWorkout')));
    //  final titleFinder = find.text('CreateWorkout');
    //  expect(titleFinder, findsOneWidget);
  });
}
