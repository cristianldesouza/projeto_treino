import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:projeto_treino/app/modules/create_workout/create_workout_module.dart';

void main() {
  initModule(CreateWorkoutModule());
  // CreateWorkoutController createworkout;
  //
  setUp(() {
    //     createworkout = CreateWorkoutModule.to.get<CreateWorkoutController>();
  });

  group('CreateWorkoutController Test', () {
    //   test("First Test", () {
    //     expect(createworkout, isInstanceOf<CreateWorkoutController>());
    //   });

    //   test("Set Value", () {
    //     expect(createworkout.value, equals(0));
    //     createworkout.increment();
    //     expect(createworkout.value, equals(1));
    //   });
  });
}
